/*import React from "react";

const project = props => {
  const emptyMessage = <p />;

  const project = projects.map(project => {
    return (
      <div className="">
        <p>{props.title}</p>
        <p>{props.description}</p>
      </div>
    );
  });

  return <div>{projects.length === 0 ? emptyMessage : project}</div>;
};

export default project;
*/
